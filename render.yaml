services:
  - type: web
    name: kp-pakse-pos
    runtime: docker
    dockerfilePath: Dockerfile
    plan: free
    domains:
      - kp-pakse-suthatpospos.shop
      - www.kp-pakse-suthatpospos.shop
    envVars:
      - key: PORT
        value: 5173
      - key: NODE_ENV
        value: production

